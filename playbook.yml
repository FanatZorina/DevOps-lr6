---
- name: Install nginx with virtual hosts
  hosts: webservers
  become: true
  become_method: sudo
  gather_facts: no

  vars:
    sites:
      - "etis.com"
      - "fizfak.com"
      - "mehmat.ru"

  roles:
    - nginx_vhosts